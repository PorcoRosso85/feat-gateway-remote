#!/usr/bin/env bash
# ssh: Direct SSH connection (ProxyJump prohibited)

if ! command -v ssh >/dev/null 2>&1; then
  echo "contracted command: ssh (openssh not available)"
  exit 1
fi

echo "contracted command: ssh (openssh)"
echo "Usage: ssh [user@]host [command]"
echo ""
echo "Note: Indirect connection methods are prohibited."

exit 0
